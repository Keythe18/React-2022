/**
 * React utilise le JSX comme modèle au lieu du Javascript habituel
 * Avantages : 
 * plus rapide car il effectue l'optimisation tout en compilant le code en Javascript
 * plus sur => la plupart  des erreurs peuvent etre détectées lors de la compilation
 * plus facile et plus rapide d'écrire des modèles 
 * 
 * attributs personnalisés =< faut utiliser un préfixe data : data-nomattribut
 * les expressions Javascript peuvent etre utilisées dans JSX il suffit de les entourer par des {}
 * on ne peut utiliser les déclarations if else dans JSX mais on peut utiliser les expressions conditionnelles (ternaires)
 * React recommande d'utiliser les inline styles , pour les définir on doit utiliser la syntaxe camelCase
 * React ajouter 'px' après la valeur numérique sur des éléments spécifiques
 * Convention d'appellation : 
 * les balises HTML utilisent toujours des noms de balises en miniscules tandis que les composants de React 
 * commencent par des Majuscules => on doit utiliser className et htmlFor comme noms d'attributs XML au lieu de class et for
 * 
 */